package com.contentmanager.app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Content Manager application.
 * This is a Spring Boot application that provides content management capabilities.
 */
@SpringBootApplication
public class ContentManagerApplication {

    public static void main(String[] args) {
        SpringApplication.run(ContentManagerApplication.class, args);
    }
}
